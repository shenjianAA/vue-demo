/**
 * 数字枚举
 */
//Up使用初始化为 1。 其余的成员会从 1开始自动增长
enum Direction {
  Up = 1,
  Down,
  Left,
  Right,
}
//Up的值为 0， Down的值为 1
enum Direction2 {
  Up = 1,
  Down,
  Left,
  Right,
}

//字符串枚举
enum Direction3 {
  Up = "UP",
  Down = "DOWN",
  Left = "LEFT",
  Right = "RIGHT",
}
